{
  "name": "playwright-automation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "tests-headless": "npx playwright test",
    "tests-headed": "npx playwright test  --headed",
    "test-tag-headless": "npx playwright test --grep @tag",
    "test-tag-headed": "npx playwright test --headed --grep @a02",
    "test-smoke": "npx playwright test --grep @smoke",
    "report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.1.0",
    "dotenv": "^17.3.1"
  }
}
